#include <zx/endian.h>
ZXCORE zxuc* zxcopybits(
  zxuc* src, size_t sbits, zxEndian sen,
  zxuc* des, size_t dbits, zxEndian den,
  size_t from, size_t to )
{
  /* TODO: Implement zxcopybits() */
  return des;
}